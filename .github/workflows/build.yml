name: Compilar APK Delicias Crisant

on:
  workflow_dispatch:
  push:
    branches: [ "principal", "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar codigo
        uses: actions/checkout@v4

      - name: Instalar dependencias Linux
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev ninja-build

      - name: Instalar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Configurar Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: 'stable'

      - name: Aceptar licencias de Android SDK
        run: |
          yes | flutter doctor --android-licenses || true

      - name: Configurar Python del sistema
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar Flet CLI y dependencias
        run: |
          python -m pip install --upgrade pip
          pip install "flet[all]" reportlab pillow

      - name: Compilar APK
        run: |
          flet build apk -v

      - name: Guardar APK como descarga
        uses: actions/upload-artifact@v4
        with:
          name: Delicias-Crisant-APK
          path: build/apk/*.apk
